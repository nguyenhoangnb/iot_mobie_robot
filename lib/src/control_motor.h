// #ifndef CONTROL_MOTOR_H
// #define CONTROL_MOTOR_H
// #include <AccelStepper.h>

// class control_motor {
// private: 
//     AccelStepper &stepper1;
//     AccelStepper &stepper2;

// public:
//     control_motor(AccelStepper &step1, AccelStepper &step2);  // Constructor with stepper references
//     void init();  // Initialize the motors
//     void run();  // Run the motors
//     void stopMotors();  // Stop the motors
//     void turnLeft(int speed);  // Turn left with given speed
//     void turnRight(int speed);  // Turn right with given speed
//     void moveForward(int speed);  // Move forward with given speed
//     void moveBackward(int speed);  // Move backward with given speed
// };

// #endif
